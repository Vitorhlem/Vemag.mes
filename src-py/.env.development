PROJECT_NAME=TruCar
API_V1_STR=/api/v1
FRONTEND_URL=http://localhost:9000
BACKEND_CORS_ORIGINS=["http://localhost", "http://localhost:8080", "http://localhost:9000"]
SUPERUSER_EMAILS=["admin@admin.com", "vitorhugolemes6@gmail.com"]

# SMTP Configuration (Dev - Mailtrap or similar recommended for real dev, using localhost for now)
SMTP_SERVER=localhost
SMTP_PORT=1025
SMTP_USER=dev_user
SMTP_PASSWORD=dev_password
EMAILS_FROM_EMAIL=dev@trucar.com

# External APIs
OPENWEATHER_API_KEY=dev_openweather_key

# Redis
REDIS_URL=redis://localhost:6379/0

# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SERVER=localhost
POSTGRES_DB=trucar_dev

# Security (Dev keys)
SECRET_KEY=dev_secret_key_insecure_for_production
ACCESS_TOKEN_EXPIRE_MINUTES=10080
REFRESH_TOKEN_SECRET_KEY=dev_refresh_key_insecure
REFRESH_TOKEN_EXPIRE_MINUTES=43200
ALGORITHM=HS256
FERNET_KEY=dev_fernet_key_must_be_url_safe_base64_encoded_32_bytes=

# Demo Limits
DEMO_TOTAL_LIMITS={"vehicles": 3, "users": 2, "parts": 15, "clients": 5, "implements": 2, "vehicle_components": 10}
DEMO_MONTHLY_LIMITS={"reports": 5, "fines": 3, "documents": 10, "freight_orders": 10, "maintenance_requests": 5, "fuel_logs": 20}
