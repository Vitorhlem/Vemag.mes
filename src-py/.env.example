PROJECT_NAME=TruCar
API_V1_STR=/api/v1
FRONTEND_URL=http://localhost:9000
BACKEND_CORS_ORIGINS=["http://localhost", "http://localhost:8080"]
SUPERUSER_EMAILS=["admin@admin.com", "vitorhugolemes6@gmail.com"]

# SMTP Configuration
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASSWORD=your_smtp_password
EMAILS_FROM_EMAIL=no-reply@trucar.com

# External APIs
OPENWEATHER_API_KEY=your_openweather_api_key

# Redis
REDIS_URL=redis://localhost:6379/0

# Database (Option 1: Individual fields)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SERVER=localhost
POSTGRES_DB=trucar

# Database (Option 2: Full URL - Overrides individual fields)
# DATABASE_URL=postgresql+asyncpg://user:pass@host:port/db

# Security
SECRET_KEY=your_secret_key_here
ACCESS_TOKEN_EXPIRE_MINUTES=10080
REFRESH_TOKEN_SECRET_KEY=your_refresh_token_secret_key_here
REFRESH_TOKEN_EXPIRE_MINUTES=43200
ALGORITHM=HS256
FERNET_KEY=your_fernet_key_here

# Demo Limits (JSON format)
# DEMO_TOTAL_LIMITS={"vehicles": 3, "users": 2, "parts": 15, "clients": 5, "implements": 2, "vehicle_components": 10}
# DEMO_MONTHLY_LIMITS={"reports": 5, "fines": 3, "documents": 10, "freight_orders": 10, "maintenance_requests": 5, "fuel_logs": 20}
